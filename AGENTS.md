# AGENTS.md

- 글 원본은 DB/CMS가 아니라 repo MDX다. DB를 붙이더라도 글 원본은 옮기지 않는다.
- 공개 URL 안정성이 중요하므로 slug는 한 번 공개하면 바꾸지 않는다.
- 새 기능은 먼저 정적 빌드로 가능한지 검토한다. 서버/DB/API는 v1 범위가 아니다.
- 댓글은 v1 범위가 아니다. 필요해지면 giscus를 selected posts에만 검토한다.
- 컴포넌트는 글 가독성을 해치면 실패다. 장식보다 타이포그래피와 코드블록을 우선한다.
- 한국어/영어 글은 한 피드에 섞되, URL은 영어 kebab-case로 유지한다.
- 코드에서 바로 읽히는 폴더 설명은 여기에 추가하지 않는다.
